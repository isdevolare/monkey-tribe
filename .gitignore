node_modules/
.expo/
dist/
web-build/

# Managed Expo project: native folders are generated by prebuild and must
# not be committed. Config stays in app.json + config plugins. The local
# ios/ folder is kept on disk for physical-device testing but stays ignored.
/ios/
/android/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Local scratch/asset-generation artifacts
tmp/
